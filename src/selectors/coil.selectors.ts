export const selectCoilTurnsCount = (state: RootState) => state.coil.turnsCount

export const selectCoilFrameWidth = (state: RootState) => state.coil.frame.width

export const selectCoilFrameLength = (state: RootState) =>
  state.coil.frame.length
