export const selectHelmholtzCoilsRadius = (state: RootState) =>
  state.HelmholtzCoils.radius

export const selectHelmholtzCoilsAmperage = (state: RootState) =>
  state.HelmholtzCoils.amperage

export const selectHelmholtzCoilsTurnsCount = (state: RootState) =>
  state.HelmholtzCoils.turnsCount
